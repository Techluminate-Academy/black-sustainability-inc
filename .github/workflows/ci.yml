# PRs to main/develop only. Branch protection requires "CI / Lint, Typecheck, Test, Build".
# Push-only runs live in ci-push.yml so that name is not split into (push)/(pull_request).
name: CI

on:
  pull_request:
    branches: [main, develop]

jobs:
  test-and-lint:
    name: Lint, Typecheck, Test, Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Test
        run: npm test
        env:
          JWT_SECRET: ci-jwt-secret-not-for-production

      - name: Build
        run: npm run build
        env:
          JWT_SECRET: ci-jwt-secret-not-for-production
          NEXTAUTH_SECRET: ci-nextauth-secret-not-for-production
